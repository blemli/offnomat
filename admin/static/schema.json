{
  "title": "Offnomat",
  "type": "object",
  "required": [
    "mode",
    "places"
  ],
  "properties": {
    "mode": {
      "type": "string",
      "description": "the mode of transport you usually use to get there",
      "enum": [
        "foot",
        "bike",
        "car"
      ],
      "default": "bike"
    },
    "graphhopper_key": {
      "type": "string",
      "title": "GraphHopper API key",
      "description": "API key from graphhopper.com for travel-time routing. Optional — without it, colors use opening hours and time-spent only (no travel time)."
    },
    "night_mode": {
      "type": "boolean",
      "format": "checkbox",
      "title": "Night mode",
      "description": "Turn the LEDs off when the light sensor reports darkness",
      "default": true
    },
    "places": {
      "type": "array",
      "format": "table",
      "title": "Places",
      "uniqueItems": true,
      "maxItems": 16,
      "items": {
        "type": "object",
        "title": "Place",
        "properties": {
          "name": {
            "type": "string"
          },
          "duration": {
            "description": "How many minutes you spend on that place at minimum",
            "type": "integer",
	    "format":"number",
            "default": 5,
            "minimum":0
          }
        }
      }
    }
  }
}
